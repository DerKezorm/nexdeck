-r requirements.txt
# 8.4.2 and older make their temporary directory readable to everyone on
# the machine (GHSA-6w46-j5rx-g56g).
pytest==9.1.1
pytest-asyncio==1.3.0
# Records HTTP conversations for adapter tests; no adapter test talks to the network.
respx==0.22.0
# Pinned exactly on purpose: ruff adds rules to its defaults with every minor
# release, and an unpinned build would turn red one day without any change.
ruff==0.16.5
