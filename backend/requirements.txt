# ⚠️ Starlette below 1.0.1 has five open advisories, among them one that
# poisons request.url.path and walks past path-based checks. FastAPI
# 0.121 capped starlette under 0.50, so closing them needed both.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.38.0
sqlalchemy==2.0.44
pydantic==2.12.4
pydantic-settings==2.14.2
bcrypt==5.0.0
pyjwt==2.13.0
httpx==0.28.1
cryptography==50.0.1
# Web Push: py-vapid signs each message for the push service (RFC 8292),
# http-ece encrypts it for the browser (RFC 8291). Both only depend on
# cryptography; pywebpush would pull in requests and aiohttp.
py-vapid==1.9.4
http-ece==1.2.1
# Board files, provisioning and export.
pyyaml==6.0.3
# Home Assistant speaks WebSocket; httpx does not.
websockets==15.0.1
# One adapter, a hundred notification services.
apprise==1.9.5
# RSS and Atom parsing for the feed widget.
feedparser==6.0.12
python-multipart==0.0.31

# The second factor. pyotp does the time codes, segno draws the QR code in
# our own process: a secret sent to a foreign QR service is not a secret.
pyotp==2.9.0
segno==1.6.6

# Backups. An AES-encrypted ZIP and not a format of our own: the archive
# carries secret.key, so it needs a password, and it has to open with 7-Zip
# on a day when nexdeck itself is what stopped working.
pyzipper==0.4.0
